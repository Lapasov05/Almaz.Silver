# TZ 16: Nginx reverse proxy. Faza 7: xavfsizlik headerlari + rate limit + TLS tayyorligi.

# Rate limit zonasi (webhook/API himoyasi — TZ 15). IP bo'yicha.
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=20r/s;

server {
    listen 80;
    server_name almaz.api.cognilabs.org _;

    # Trafik tashqi edge (cims_nginx, 443) orqali keladi — haqiqiy mijoz IP'sini
    # X-Real-IP'dan olamiz, aks holda rate limit hamma so'rovni bitta (edge) IP deb ko'radi.
    set_real_ip_from 172.16.0.0/12;
    set_real_ip_from 65.109.136.247;
    real_ip_header X-Real-IP;

    # Chek rasmi / media yuklash uchun yetarli limit
    client_max_body_size 20M;

    # --- Xavfsizlik headerlari (TZ 15) ---
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    # HTTPS yoqilganда: Strict-Transport-Security (quyida TLS blokida)

    location / {
        limit_req zone=api_limit burst=40 nodelay;

        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- TLS (TZ 15: transit shifrlash). Sertifikat o'rnatilgach yoqiladi ---
# server {
#     listen 443 ssl http2;
#     server_name your-domain.uz;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     client_max_body_size 20M;
#     location / {
#         limit_req zone=api_limit burst=40 nodelay;
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
